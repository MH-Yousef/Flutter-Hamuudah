import 'dart:math';

void main(List<String> args) {
  // max ve min kullanımı :
  // max() fonksiyonu içine yazılan sayıların en büyüğünü verir.
  // max ve min fonksiyonlarını kullanırken sayılar arasında virgül kullanılmalıdır.
  print(max(10, 20));
  print(min(10, 20));
}
