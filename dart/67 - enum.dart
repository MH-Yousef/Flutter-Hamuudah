void main() {
  String gun = "pazartesi";

  print(gun);
}

// enum nedir ?
// enum, sabitlerin bir araya getirilmesiyle oluşturulan bir veri tipidir.
// enum kullanımı :
enum EnumAdi { eleman1, eleman2, eleman3, eleman4, eleman5 }

// enum kullanımı örneği :
enum Gunler { pazartesi, sali, carsamba, persembe, cuma, cumartesi, pazar }

// enum kullanımı örneği :
enum Aylar { ocak, subat, mart, nisan, mayis, haziran, temmuz, agustos, eylul, ekim, kasim, aralik }

// enum kullanımı örneği :
enum Sayilar { sifir, bir, iki, uc, dort, bes, alti, yedi, sekiz, dokuz, on }
