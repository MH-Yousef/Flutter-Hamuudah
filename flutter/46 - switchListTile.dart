void main(List<String> args) {
  // switchListTile widget nedir?
  // örnek
  /*
  body: Container(
          padding: const EdgeInsets.all(10),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Center(child: Text("bildirimleri açmak ister misiniz?")),
              SwitchListTile(
                  value: deger,
                  onChanged: (val) {
                    setState(() {
                      deger = val;
                      print(deger);
                    });
                  }),
            ],
          ),
        ),
  */

  // write notification function switchListTile
  // 


}
