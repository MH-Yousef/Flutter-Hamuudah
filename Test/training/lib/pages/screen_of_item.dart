import 'package:flutter/material.dart';

class ScreenOfItem extends StatelessWidget {
  const ScreenOfItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
